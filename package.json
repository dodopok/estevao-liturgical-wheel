{
  "name": "liturgical-wheel",
  "version": "1.2.0",
  "description": "O Ano Litúrgico — Calendário do Ano Cristão",
  "private": true,
  "_comment_scripts": "Não crie um script chamado \"dev\": sem framework detectado, o `vercel dev` executa o script `dev` do package.json e aborta com erro de recursão.",
  "scripts": {
    "serve": "vercel dev",
    "deploy": "vercel --prod",
    "test:print": "node scripts/print-test.js",
    "og-image": "node scripts/og-image.js"
  },
  "devDependencies": {
    "puppeteer-core": "^24.39.0",
    "vercel": "^56.5.0"
  }
}
